# Gmail account that will receive the digest
RECIPIENT_EMAIL=your-email@gmail.com

# n8n Basic Authentication
# Set these to secure your n8n instance
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme_use_strong_password

# n8n Host Configuration
# For local development, use localhost
# For production with domain, use your domain name
N8N_HOST=localhost
N8N_PROTOCOL=http




# -----------------------------------------------------------------------------
# Optional: Customize Search Criteria
# -----------------------------------------------------------------------------
# Minimum stars for repositories
MIN_STARS=50

# Days to look back
LOOKBACK_DAYS=7

# Topics to search (comma-separated)
SEARCH_TOPICS=generative-ai,llm,gpt,stable-diffusion,langchain

# Maximum repositories to include in digest
MAX_REPOS=10

# Gmail App Password (not your regular password!)
# Create at: https://myaccount.google.com/apppasswords
# Requires 2FA to be enabled
# Only needed if you don't use OAuth2
GMAIL_APP_PASSWORD=your_16_char_app_password

# GitHub API Token (Optional - can also be set via n8n credentials UI)
# Get one at: https://github.com/settings/tokens
# Required scope: public_repo
GITHUB_TOKEN=ghp_your_github_token_here

# OpenAI API Key (Optional - can also be set via n8n credentials UI)
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here

# Timezone (optional, defaults to UTC)
# Examples: America/New_York, Europe/London, Asia/Tokyo
TIMEZONE=UTC